{
  "name": "premium-producer-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-site.mjs",
    "preview": "npm run build && python3 -m http.server 4173 --directory dist"
  }
}
